name: App Infra Pipeline

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [ main ]
permissions:
  id-token: write
  contents: read
jobs:
  dev:
    uses: seera-eswara/github-actions-templates/.github/workflows/terraform-iac.yml@v1.0.0
    with:
      environment: dev
      policy_repo: seera-eswara/terraform-policy-as-code
      policy_path: policies/
    secrets: inherit
    permissions:
      id-token: writees
      contents: write



  prod:
    needs: dev
    uses: seera-eswara/github-actions-templates/.github/workflows/terraform-iac.yml@v1.0.0
    with:
      environment: prod
      policy_repo: seera-eswara/terraform-policy-as-code
      working_directory: environments/prod
      policy_path: policies/
    secrets: inherit
    permissions:
      id-token: write
      contents: read

